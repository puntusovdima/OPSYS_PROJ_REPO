Dmitrii Puntusov
Samuel Porta

To execute the program use basic gcc compiler and complile it in the same directory as particion.bin file.
Use the commands: [info, bytemaps, dir, rename, print, remove, copy, exit].
After the you finished update the .bin file using the 'exit' command.
